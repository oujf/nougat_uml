class TouchAffineTransformation
!!!40790967.cpp!!!	applyTo(inout x : float, inout y : float) : void
    float newX, newY;
    newX = x * x_scale + y * x_ymix + x_offset;
    newY = x * y_xmix + y * y_scale + y_offset;

    x = newX;
    y = newY;
!!!85821367.cpp!!!	applyTo(inout x : float, inout y : float) : void
    float newX, newY;
    newX = x * x_scale + y * x_ymix + x_offset;
    newY = x * y_xmix + y * y_scale + y_offset;

    x = newX;
    y = newY;
