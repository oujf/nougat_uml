format 213
"backup" // android::frameworks::base::cmds::backup
  revision 1
  modified_by 55 "Jeff"
  // class settings
  
  classdiagramsettings member_max_width 0 end
  
  usecasediagramsettings end
  
  sequencediagramsettings end
  
  collaborationdiagramsettings end
  
  objectdiagramsettings end
  
  componentdiagramsettings
   end
  
  deploymentdiagramsettings
   end
  
  statediagramsettings
   end
  
  activitydiagramsettings
   end
  
  cpp_h_dir "D:/Workspace/AndroidSourceCode/nougat_src/frameworks/base/cmds/backup/"
  cpp_src_dir "D:/Workspace/AndroidSourceCode/nougat_src/frameworks/base/cmds/backup/"
  deploymentview 429111 "backup"
    //deployment diagram settings
    deploymentdiagramsettings
     end
    artifact 3000631 "main"
      stereotype "source"
      cpp_src "int main(int argc, const char** argv)
{
    if (argc <= 1) {
        return perform_full_backup();
    }
    if (argc == 3 && 0 == strcmp(argv[1], \"list\")) {
        return perform_list(argv[2]);
    }
    if (argc == 4 && 0 == strcmp(argv[1], \"print\")) {
        return perform_print(argv[2], argv[3]);
    }
    return usage(argc, argv);
}
"
      associated_classes
      end
    end
  end
end
