class StrictModeBenchmark
!!!47746359.java!!!	timeToggleThreadPolicy(in reps : int) : void
        for (int i = 0; i < reps; i++) {
            StrictMode.setThreadPolicy(mOn);
            StrictMode.setThreadPolicy(mOff);
        }
