class MainActivity
!!!68868151.java!!!	onCreate(inout savedInstanceState : Bundle) : void
        super.onCreate(savedInstanceState);
        TextView tv = new TextView(this);
        int sum = Native.add(2, 3);
        tv.setText("[computed by 32 bit native code] 2 + 3 = " + String.valueOf(sum));
        setContentView(tv);
