class PlaybackInfo
!!!69784503.java!!!	PlaybackInfo(in state : int, in stateChangeTimeMs : long, in currentPosMs : long, in speed : float)
            mState = state;
            mStateChangeTimeMs = stateChangeTimeMs;
            mCurrentPosMs = currentPosMs;
            mSpeed = speed;
