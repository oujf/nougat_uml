class GLES30
!!!70570807.java!!!	glVertexAttribIPointer(in index : int, in size : int, in type : int, in stride : int, inout pointer : java.nio.Buffer) : void
        glVertexAttribIPointerBounds(
            index,
            size,
            type,
            stride,
            pointer,
            pointer.remaining()
        );
