class ClearReceiver
!!!71526071.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
            if (ACTION_CLEAR.equals(intent.getAction())) {
                get(context).clearAll();
            }
