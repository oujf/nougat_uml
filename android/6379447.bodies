class VelocityInterpolator
!!!71990199.java!!!	VelocityInterpolator(in durationSeconds : float, in velocity : float, in diff : float)
            mDurationSeconds = durationSeconds;
            mVelocity = velocity;
            mDiff = diff;
!!!71990327.java!!!	getInterpolation(in input : float) : float
            float time = input * mDurationSeconds;
            return time * mVelocity / mDiff;
