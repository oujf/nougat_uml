class KernelID
!!!72770615.java!!!	KernelID(in id : long, inout rs : RenderScript, inout s : Script, in slot : int, in sig : int)
            super(id, rs);
            mScript = s;
            mSlot = slot;
            mSig = sig;
            guard.open("destroy");
