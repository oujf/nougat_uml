class LogSSC
!!!73234487.java!!!	set(inout t : Time, inout s : String, in subId : int, in phoneId : int, inout state : ServiceState) : void
            mTime = t; mS = s; mSubId = subId; mPhoneId = phoneId; mState = state;
!!!73234615.java!!!	toString() : String
            return mS + " Time " + mTime.toString() + " mSubId " + mSubId + " mPhoneId "
                    + mPhoneId + "  mState " + mState;
