class PathDestructionActivity
!!!75809719.java!!!	onCreate(inout savedInstanceState : Bundle) : void
        super.onCreate(savedInstanceState);

        MyView view = new MyView(this);
        setContentView(view);
