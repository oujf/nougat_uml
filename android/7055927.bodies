class AbstractTimePickerDelegate
!!!78233399.java!!!	AbstractTimePickerDelegate(inout delegator : TimePicker, inout context : Context)
            mDelegator = delegator;
            mContext = context;
            mLocale = context.getResources().getConfiguration().locale;
