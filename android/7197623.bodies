class SoundTrigger
!!!79680311.java!!!	attachModule(in moduleId : int, inout listener : SoundTrigger::StatusListener, inout handler : Handler) : SoundTriggerModule
        if (listener == null) {
            return null;
        }
        SoundTriggerModule module = new SoundTriggerModule(moduleId, listener, handler);
        return module;
