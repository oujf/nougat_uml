class EventLogTags
!!!80969783.java!!!	writeTtsSpeakSuccess(inout engine : String, in callerUid : int, in callerPid : int, in length : int, inout locale : String, in rate : int, in pitch : int, in engineLatency : long, in engineTotal : long, in audioLatency : long) : void
    android.util.EventLog.writeEvent(TTS_SPEAK_SUCCESS, engine, callerUid, callerPid, length, locale, rate, pitch, engineLatency, engineTotal, audioLatency);
!!!80969911.java!!!	writeTtsSpeakFailure(inout engine : String, in callerUid : int, in callerPid : int, in length : int, inout locale : String, in rate : int, in pitch : int) : void
    android.util.EventLog.writeEvent(TTS_SPEAK_FAILURE, engine, callerUid, callerPid, length, locale, rate, pitch);
!!!80970039.java!!!	writeTtsV2SpeakSuccess(inout engine : String, in callerUid : int, in callerPid : int, in length : int, inout requestConfig : String, in engineLatency : long, in engineTotal : long, in audioLatency : long) : void
    android.util.EventLog.writeEvent(TTS_V2_SPEAK_SUCCESS, engine, callerUid, callerPid, length, requestConfig, engineLatency, engineTotal, audioLatency);
!!!80970167.java!!!	writeTtsV2SpeakFailure(inout engine : String, in callerUid : int, in callerPid : int, in length : int, inout requestConfig : String, in statuscode : int) : void
    android.util.EventLog.writeEvent(TTS_V2_SPEAK_FAILURE, engine, callerUid, callerPid, length, requestConfig, statuscode);
