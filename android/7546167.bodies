class PssAggr
!!!82922679.java!!!	add(in newPss : long, in newSamples : long) : void
            pss = (long)( (pss*(double)samples) + (newPss*(double)newSamples) )
                    / (samples+newSamples);
            samples += newSamples;
