class ActionMenuPresenterCallback
!!!83111351.java!!!	onOpenSubMenu(inout subMenu : MenuBuilder) : boolean
            Callback cb = getCallback();
            if (cb != null) {
                cb.onMenuOpened(FEATURE_ACTION_BAR, subMenu);
                return true;
            }
            return false;
!!!83111479.java!!!	onCloseMenu(inout menu : MenuBuilder, inout allMenusAreClosing : boolean) : void
            checkCloseActionMenu(menu);
