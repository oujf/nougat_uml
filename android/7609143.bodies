class DnsServerEntry
!!!83555895.java!!!	DnsServerEntry(inout address : InetAddress, in expiry : long)
        this.address = address;
        this.expiry = expiry;
!!!83556023.java!!!	compareTo(inout other : DnsServerEntry) : int
        return Long.compare(other.expiry, this.expiry);
