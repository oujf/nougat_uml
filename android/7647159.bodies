class IntInMatcher
!!!83839799.java!!!	IntInMatcher(inout values : int [[]])
            Preconditions.checkNotNull("values", values);
            mValues = new ArrayList<>(values.length);
            for (int i : values) {
                mValues.add(i);
            }
