class BooleanInMatcher
!!!83840183.java!!!	BooleanInMatcher(inout values : boolean [[]])
            Preconditions.checkNotNull("values", values);
            mValues = new ArrayList<>(values.length);
            for (boolean i : values) {
                mValues.add(i);
            }
