class Record
!!!84193079.java!!!	matchPhoneStateListenerEvent(in events : int) : boolean
            return (callback != null) && ((events & this.events) != 0);
!!!84193207.java!!!	matchOnSubscriptionsChangedListener() : boolean
            return (onSubscriptionsChangedListenerCallback != null);
!!!84193335.java!!!	toString() : String
            return "{callingPackage=" + callingPackage + " binder=" + binder
                    + " callback=" + callback
                    + " onSubscriptionsChangedListenererCallback="
                                            + onSubscriptionsChangedListenerCallback
                    + " callerUserId=" + callerUserId + " subId=" + subId + " phoneId=" + phoneId
                    + " events=" + Integer.toHexString(events)
                    + " canReadPhoneState=" + canReadPhoneState + "}";
