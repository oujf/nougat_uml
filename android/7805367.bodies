class H
!!!85424695.java!!!	handleMessage(inout msg : Message) : void
            switch(msg.what) {
                case SHOW:
                    handleShow();
                    break;
                case HIDE:
                    handleHide();
                    break;
            }
