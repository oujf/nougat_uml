class IntInMatcher
!!!29083831.java!!!	IntInMatcher(inout values : int)
            Preconditions.checkNotNull("values", values);
            mValues = new ArrayList<>(values.length);
            for (int i : values) {
                mValues.add(i);
            }
