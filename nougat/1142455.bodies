class DummyCameraServiceListener
!!!29116343.java!!!	onStatusChanged(in status : int, in cameraId : int) : void
            Log.v(TAG, String.format("Camera %d has status changed to 0x%x", cameraId, status));
!!!29116471.java!!!	onTorchStatusChanged(in status : int, inout cameraId : String) : void
            Log.v(TAG, String.format("Camera %s has torch status changed to 0x%x",
                    cameraId, status));
