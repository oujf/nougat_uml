class ClearReceiver
!!!30450615.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
            if (ACTION_CLEAR.equals(intent.getAction())) {
                get(context).clearAll();
            }
