class H
!!!30878647.java!!!	handleMessage(inout msg : Message) : void
            final int what = msg.what;
            switch (what) {
                case MSG_SHORTCUT_RECEIVED:
                    mCallbacks.onShortcutKeyPressed((Long)msg.obj);
                    break;
            }
