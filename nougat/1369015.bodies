class VelocityInterpolator
!!!30969911.java!!!	VelocityInterpolator(in durationSeconds : float, in velocity : float, in diff : float)
            mDurationSeconds = durationSeconds;
            mVelocity = velocity;
            mDiff = diff;
!!!30970039.java!!!	getInterpolation(in input : float) : float
            float time = input * mDurationSeconds;
            return time * mVelocity / mDiff;
