class CstLiteralBits
!!!609079.java!!!	fitsIn16Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (short) bits == bits;
!!!609207.java!!!	fitsIn8Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (byte) bits == bits;
