class ViewConfiguration_Accessor
!!!35369143.java!!!	clearConfigurations() : void
        // clear the stored ViewConfiguration since the map is per density and not per context.
        ViewConfiguration.sConfigurations.clear();
