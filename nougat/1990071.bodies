class ColorPaletteDrawable
!!!36319159.java!!!	ColorPaletteDrawable(in color : int, in width : int, in height : int, in mergin : int)
            super(new RectShape());
            mRect = new Rect(mergin, mergin, width - mergin, height - mergin);
            getPaint().setColor(color);
!!!36319287.java!!!	draw(inout canvas : Canvas) : void
            canvas.drawRect(mRect, getPaint());
