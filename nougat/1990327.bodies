class CopyAction
!!!36322615.java!!!	doEndPosIsSelected() : boolean
                if (super.doEndPosIsSelected()) {
                    return true;
                }
                mManager.copyToClipBoard();
                mManager.resetEdit();
                return true;
