class CutAction
!!!36322743.java!!!	doEndPosIsSelected() : boolean
                if (super.doEndPosIsSelected()) {
                    return true;
                }
                mManager.cutToClipBoard();
                mManager.resetEdit();
                return true;
