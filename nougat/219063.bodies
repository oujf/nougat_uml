class CstLiteralBits
!!!1005239.java!!!	fitsIn16Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (short) bits == bits;
!!!1005367.java!!!	fitsIn8Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (byte) bits == bits;
