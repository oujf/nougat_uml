class ViewHolder
!!!38457783.java!!!	setupViewHolder(inout v : View) : void
            ViewHolder vh = new ViewHolder();
            vh.strTextView = (TextView) v.findViewById(R.id.value);
            v.setTag(vh);
