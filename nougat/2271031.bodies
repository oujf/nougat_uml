class MathUtils
!!!38817975.java!!!	constrain(in amount : int, in low : int, in high : int) : int
        return amount < low ? low : (amount > high ? high : amount);
!!!38818103.java!!!	constrain(in amount : float, in low : float, in high : float) : float
        return amount < low ? low : (amount > high ? high : amount);
