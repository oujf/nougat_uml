class AppWidget
!!!1299511.java!!!	main(inout args : String) : void
        Parser parser = new Parser(args);
        Runnable command = parser.parseCommand();
        if (command != null) {
            command.run();
        }
