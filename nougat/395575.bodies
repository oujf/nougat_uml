class AbstractTimePickerDelegate
!!!2314807.java!!!	AbstractTimePickerDelegate(inout delegator : TimePicker, inout context : Context)
            mDelegator = delegator;
            mContext = context;
            mLocale = context.getResources().getConfiguration().locale;
