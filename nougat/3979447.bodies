class H
!!!42831415.java!!!	handleMessage(inout msg : Message) : void
            switch(msg.what) {
                case SHOW:
                    handleShow();
                    break;
                case HIDE:
                    handleHide();
                    break;
            }
