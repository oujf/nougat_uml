class AnglesPercentageEvaluator
!!!44438199.java!!!	evaluate(in value : float) : float
        float evaluation = 0.0f;
        if (value < 1.00) evaluation++;
        if (value < 0.90) evaluation++;
        if (value < 0.70) evaluation++;
        return evaluation;
