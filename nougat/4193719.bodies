class SpeedVarianceEvaluator
!!!44454967.java!!!	evaluate(in value : float) : float
        float evaluation = 0.0f;
        if (value > 0.06) evaluation++;
        if (value > 0.15) evaluation++;
        if (value > 0.3) evaluation++;
        if (value > 0.6) evaluation++;
        return evaluation;
