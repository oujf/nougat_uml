class VelocityInterpolator
!!!44873399.java!!!	VelocityInterpolator(in durationSeconds : float, in velocity : float, in diff : float)
            mDurationSeconds = durationSeconds;
            mVelocity = velocity;
            mDiff = diff;
!!!44873527.java!!!	getInterpolation(in input : float) : float
            float time = input * mDurationSeconds;
            return time * mVelocity / mDiff;
