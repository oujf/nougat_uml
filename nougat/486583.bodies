class DisplayManagerCallback
!!!3232311.java!!!	onDisplayEvent(in displayId : int, in event : int) : void
            if (DEBUG) {
                Log.d(TAG, "onDisplayEvent: displayId=" + displayId + ", event=" + event);
            }
            handleDisplayEvent(displayId, event);
