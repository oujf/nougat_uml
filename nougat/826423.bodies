class OpenFromApkActivity
!!!6585015.java!!!	onCreate(inout savedInstanceState : Bundle) : void
        super.onCreate(savedInstanceState);

        TextView  tv = new TextView(this);

        int i = JNITests.checkFunction();

        tv.setText("All is well: i=" + i);

        setContentView(tv);
