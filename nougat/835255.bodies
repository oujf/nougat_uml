class Idler
!!!6636215.java!!!	queueIdle() : boolean
            //Message m = Message.obtain();
            //mHandler.sendMessageAtTime(m, SystemClock.uptimeMillis()+1000);
            setResult(RESULT_OK);
            finish();
            return false;
