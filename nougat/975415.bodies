class AvoidXfermode
!!!27729207.java!!!	AvoidXfermode(in opColor : int, in tolerance : int, inout mode : AvoidXfermode::Mode)
        if (tolerance < 0 || tolerance > 255) {
            throw new IllegalArgumentException("tolerance must be 0..255");
        }
