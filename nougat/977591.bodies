class EmbossMaskFilter
!!!27760695.java!!!	EmbossMaskFilter(inout direction : float, in ambient : float, in specular : float, in blurRadius : float)
        if (direction.length < 3) {
            throw new ArrayIndexOutOfBoundsException();
        }
        native_instance = nativeConstructor(direction, ambient, specular, blurRadius);
